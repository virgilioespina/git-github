Create Repo
'git init'

Sycning Repo
add remote repo: git add master <link>
pull files: 'git pull'
push  your own changes in centrol repo: 'git push'

Making Changes
git status
add a file: git add <file name>
add all files: git add -A 

commit a file: git commit -m "<message>"
commit all files: git commit -a -m "<message>"

git log 

Parallel Development - Branching
Branches are pointers to a specific commit
Branches are of two types:
	- Local Branches
	- Remote-tracking branches

git checkout <name of the branch>
merging to the main branch: git merge <branch name>   checkout to the main branch first
cat <file name>



